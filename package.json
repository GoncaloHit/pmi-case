{
  "private": true,
  "workspaces": [
    "./packages/api",
    "./packages/app",
    "./packages/auth",
    "./packages/ui"
  ],
  "scripts": {
    "app:dev": "yarn workspace app dev",
    "auth:dev": "yarn workspace auth start:dev",
    "api:dev": "yarn workspace api start:dev",
    "ui:build": "yarn workspace ui build",
    "ui:storybook": "yarn workspace ui storybook"
  }
}